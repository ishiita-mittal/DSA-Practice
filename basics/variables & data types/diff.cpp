#include <iostream>
using namespace std;
                    
int main() {
       int a, b, diff;
       cout<<"Enter a & b:";
       cin>>a>>b;
       diff=a-b;
       cout<<"diff = "<<diff;     
    return 0;
}